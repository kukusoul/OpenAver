[pytest]
pythonpath = .
testpaths = tests
# -n auto：依 CPU 核心數開 worker（本機 14 核實測 unit 186s→58s、integration 161s→47s）。
# --dist loadfile：同一檔案的測試固定落在同一個 worker，保住「檔內執行順序」。
#   這不是效能微調，是正確性需求——本套件至少有一支測試依賴同檔前面測試留下的狀態
#   （test_api_scanner.py::TestScannerAPI::test_get_video_unc_outside_allowlist_still_403，
#   在 6de8e7ea 單獨跑就會紅，是既有的測試隔離問題）。預設的 --dist load 會把它打散到
#   別的 worker 而失敗。哪天那支測試修好了，可以再評估要不要換回 load。
# 要關掉平行（除錯、--pdb、想看連續輸出時）：pytest -n0 ...
addopts = -v -n auto --dist loadfile
asyncio_mode = auto
markers =
    smoke: tests that connect to external services (deselect with '-m "not smoke"')
    integration: marks tests that require external services
    e2e: browser end-to-end tests (require real network; run in a SEPARATE process via `pytest tests/e2e` — never bundled with async tests in one `pytest tests/` run: pytest-playwright's session `playwright` fixture leaves a running asyncio loop that breaks pytest-asyncio Runner. deselect with '-m "not e2e"')
    perf: performance benchmarks (deselect with '-m "not perf"')
    allow_real_db: explicitly permits a test to bypass the repo-write G1 guard (sqlite3.connect real-DB / repo-root path check) — see TASK-127b-T3/T5; applied to TestWrapperArgumentBinding::test_positional_uri_false_still_rejects_repo_relative (T5's reverse-lock fix, see TASK-127b-T5.md 技術要點①-c)
