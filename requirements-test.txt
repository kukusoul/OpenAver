# 測試/CI 環境 = runtime 依賴（exact-pinned、可重現）+ 純測試/開發工具。
# 以 `-r requirements.txt` 繼承 runtime 鎖版，確保 test job（CI 只裝本檔）跑在與
# runtime/build 完全相同的 pinned 版本（含 starlette / pydantic / pywebview 等
# runtime-only 依賴），杜絕「漏鏡像到 test 檔」造成的版本漂移（TASK-79-T6 / Codex 修正）。
-r requirements.txt

# Testing & Dev（僅測試/CI 用，不進 runtime）
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-mock==3.15.1
pytest-cov==7.1.0  # US4 cov-floor (scripts/run_cov.sh)
pytest-xdist==3.8.0  # 平行測試（pytest.ini 的 addopts 預設帶 -n auto，缺這支 pytest 會因未知參數直接啟動失敗）
pytest-playwright==0.7.2  # e2e testing
PyYAML==6.0.3  # CI workflow guard (test_ci_workflow_guard.py uses yaml.safe_load)
ruff==0.15.17  # pinned exact: ruff gates PRs in CI; upstream auto-upgrade can silently change rule verdicts
import-linter==2.13  # pinned exact: CI 用它擋 PR（core ⇏ web 分層契約），上游升版可能改變契約判定
